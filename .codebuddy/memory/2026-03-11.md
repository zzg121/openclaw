# 2026-03-11 工作日志

## 生成 openclaw 记忆插件方案 v3

基于以下两个文档合并生成了 `extensions/memory-tdai/docs/openclaw记忆插件方案.md` 的 v3 版本：
- **v2 原文**: `extensions/memory-tdai/docs/openclaw记忆插件方案.md` — 四层记忆架构（L0/L1/L2/L3）完整设计
- **改造方案**: `extensions/memory-tdai/docs/memory-tdai改造方案.md` — 基于 v2 讨论的调整决策

### v2 → v3 主要变更点
1. **插件名**: memory-smart → memory-tdai（基于现有 memory-tdai 改造）
2. **数据存储路径**: `~/.openclaw/agents/<agentId>/memory/auto/` → `~/.openclaw/memory-tdai/`
3. **不依赖 memory-core**: L0/L1 向量检索自行实现，不使用 memory-core 自动索引
4. **L1/L2/L3 独立触发**: 由 ExtractionScheduler 统一调度（优先级队列），非级联触发
5. **配置结构**: 从扁平 configSchema 改为 `mode` + `common/local/remote` 三层
6. **远程模式代码暂时删除**
7. **暂不区分 actor_id**
8. **L1 目录名**: `memories/` → `records/`
9. **Checkpoint 结构**: 复用 memory-tdai 现有字段 + 扩充 L0/L1 相关字段
10. **代码结构**: 复用 memory-tdai 现有的 scene/persona/hooks 等目录，新增 conversation/ 和 record/

输出文件: `extensions/memory-tdai/docs/openclaw记忆插件方案v3.md`

### v3 文档生成完成

文件已成功写入，共 1531 行，包含以下章节：
- 一~三章：设计背景、四层架构总览、文件目录结构（含代码结构）
- 四章：各层详细设计（L0/L1/L2/L3 + Auto-Recall），含完整代码示例和 Prompt
- 五~六章：工具与CLI、配置（mode + common/local/remote 三层结构）
- 七章：ExtractionScheduler 统一调度（独立触发、优先级队列、各层 Runner）
- 八章：Checkpoint 持久化（复用原有字段 + L0/L1/L2/L3 扩充）
- 九~十章：LLM 开销分析、可配置降级策略
- 十一~十四章：云端扩展预留、对比总览、开发计划、风险缓解
- 附录 A：插件与 OpenClaw 核心的关系 + 设计决策总结
